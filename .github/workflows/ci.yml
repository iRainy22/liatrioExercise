name: ci

on:
  push:
    branches: 
      - "main"
  pull-request:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Copy Files
        uses: actions/checkout@v4

      - name: Build image
        uses: docker build -t api .
